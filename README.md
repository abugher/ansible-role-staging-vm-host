This role prepares a system to host staging VMs.  (It will be a "host" in the
sense that it will have guest VMs, not in the sense that any computer system is
some kind of "host", having a "hostname", etc.)
